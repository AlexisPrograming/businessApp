# Clearpath Finance — Environment variables
# 1. Copy this file to .env:  cp .env.example .env
# 2. Get an API key from https://console.anthropic.com
# 3. Paste it below (no quotes), then restart: npm run dev

# Anthropic API key for the AI chat assistant (required for AI to work).
VITE_ANTHROPIC_API_KEY=
