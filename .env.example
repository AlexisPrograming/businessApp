# Clearpath Finance — Environment variables
# Copy this file to .env and fill in your values.

# Anthropic API key for the AI chat assistant (optional).
# Without it, the app works but the AI advisor will ask you to add the key.
VITE_ANTHROPIC_API_KEY=
